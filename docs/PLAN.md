# План работ

Рабочий документ: что сделано, что следующее, какие решения приняты.
Требования и обоснования — в [PRD.md](PRD.md); задачи ссылаются на его `FR-N`.

**В этот файл не пишем, что именно было изменено** — это `git log` и диффы, они
точнее и не врут. Здесь живёт то, чего в истории нет: что дальше и почему так.

---

## Сейчас

- **Фаза:** 0 — Зачистка (не начата)
- **Последняя итерация:** форк создан, документы заведены
- **Следующее:** удалить карточную логику сервера (первый блок фазы 0)
- **Блокирует:** ничего. Открытый вопрос про общение (см. журнал) блокирует
  оценку сроков, но не начало работ

---

## Фаза 0 — Зачистка

Ориентир готовности: зелёный `./run typecheck`, лобби поднимается, стол пустой.

**Сервер**
- [ ] Удалить `src/server/helpers/cardActions/**` (30 файлов)
- [ ] Удалить `panicActions.ts`, `tradeCard.ts`, `discardCard.ts`, `cardHelpers.ts`, `validators.ts`, `playerAction.ts`
- [ ] Удалить `formatters/formatCardActions.ts`, `formatters/formatHand.ts`
- [ ] Вычистить из `models/Game.ts`: колоду, добор, панику, обмен, карантин, `killPlayer` и выбывание
- [ ] Вычистить из `models/Player.ts`: `hand`, `isThing`/`isInfected`/`quarantine`, `getCardTargets`, `getNextAlivePlayer`
- [ ] Отключить аналитику заглушкой — рекордер и контракт переписываются в фазе 4

**Общие типы**
- [ ] Удалить `shared/constant/cards.ts`, `cardNames.ts`, `cardSections.ts`
- [ ] Удалить `shared/enum/cards.ts`, `shared/interfaces/cards.ts`, `cardMenu.ts`
- [ ] Удалить `shared/interfaces/turnContext.ts`, `shared/enum/turnContextType.ts`, `helpers/playerHelpers.ts`

**Клиент**
- [ ] Удалить `Hand/`, `Deck/`, `Card/`, `PanicCard/`, `TableMenu/CardsCatalog.tsx`, `hint/`
- [ ] Удалить `Room/Burn.tsx`, `Deflect.tsx`, `CardFlight.tsx`, `CardDraw.tsx`, `CardEffect.tsx`, `pixiPrimitives/Fire.tsx`
- [ ] Вычистить `controllers/gameController.ts`: руку, паники, полёты карт, костёр
- [ ] Удалить ассеты карт (`resources/cards/`) и звуки карточных эффектов

**Тесты**
- [ ] Удалить `_integration/__tests__/cardLogic/**`, `deckLogic.test.ts`, `tradeLogic.test.ts`
- [ ] Удалить `e2e/cards/**`, сохранив обвязку лобби и старта партии

---

## Фаза 1 — Ядро правил

Только сервер, без интерфейса. Проверяется юнит-тестами на фиксированных сидах.

- [ ] `shared/constant/resistance.ts`: `SPY_COUNT`, `TEAM_SIZE`, `failsNeeded`, порог побед, лимит отклонений
- [ ] `shared/enum/phase.ts` и `shared/interfaces/resistanceState.ts` по модели из PRD
- [ ] Проверка диапазона 5–10 игроков при старте — `FR-1`
- [ ] Раздача ролей в `gameStarter.ts`, шпионы знают друг друга — `FR-2`
- [ ] Выбор первого лидера и передача по кругу, в том числе после отклонения — `FR-3`
- [ ] Фаза `teamBuilding`: набор ровно нужного числа игроков — `FR-4`
- [ ] Фаза `voting`: сбор голосов и подсчёт строгого большинства — `FR-5`, `FR-6`
- [ ] Счётчик отклонений, победа шпионов на пятом, обнуление после миссии — `FR-7`
- [ ] Фаза `mission`: ролевое меню карт — `FR-8`
- [ ] Вскрытие миссии числом провалов, с учётом правила двух — `FR-9`
- [ ] Условия победы и раскрытие ролей — `FR-10`
- [ ] Юнит-тест на каждое FR, сид фиксирован во всех сценариях

---

## Фаза 2 — Протокол и играбельный минимум

Ориентир готовности: партию можно доиграть до конца, пусть и некрасиво.

- [ ] Новый payload `formatUpdatePlayerPayload` под состояние партии
- [ ] `formatPlayer` под роли: шпион видит шпионов, остальные — никого — `FR-2`
- [ ] Голоса не уходят в payload до вскрытия, наружу только «ответил / не ответил» — `FR-5`
- [ ] Параллельный опрос стола: `askDecision` для нескольких игроков разом
- [ ] `formatTimerNotification` принимает `playerId[]`, `ActionTimer` показывает всех ожидаемых
- [ ] `gameController` под новое состояние
- [ ] Минимальный трек миссий и кнопки голосования через существующий `Notifier`
- [ ] Автоответ по таймауту во всех фазах — `FR-11`
- [ ] Реконнект возвращает игрока в его текущую фазу и окно — `FR-12`

---

## Фаза 3 — Стол

- [ ] Маркер лидера на бейдже игрока
- [ ] Стрелки лидер → команда (переиспользовать `helpers/arrowPath.ts`)
- [ ] Трек миссий: исходы, текущая, размер команды, пометка «×2 провала»
- [ ] Счётчик отклонений, красное пятое деление
- [ ] Одновременное вскрытие жетонов голосов у всех бейджей
- [ ] Вскрытие миссии: перемешивание и переворот карт в центре
- [ ] Раскрытие ролей на развязке — `FR-10`
- [ ] Метки игроков под новый набор значений — `FR-13`
- [ ] Звук: голосование, вскрытие, развязка

---

## Фаза 4 — Боты, аналитика, e2e

- [ ] Боты играют все фазы (набор команды, голос, карта миссии)
- [ ] Параллельные ответы ботов в одновременных фазах вместо очереди по секунде
- [ ] Новый контракт аналитики: голоса, составы команд, исходы миссий
- [ ] Фаззер натравлен на движок правил, зависших состояний нет
- [ ] e2e: полная партия впятером
- [ ] Прогнаны все критерии приёмки из PRD, раздел 10

---

## Журнал решений

**2026-08-15 · Форк, а не абстракция «движок ↔ правила».**
Игр планируется две, а не три и больше. Обобщать движок ради второй игры дороже,
чем скопировать репозиторий. Решение пересмотреть, если появится третья игра.

**2026-08-15 · База форка — `master` (`16d39d4`).**
Ветка `small-arrow-fix` целиком входит в `master`, отдельно её тянуть не нужно.
`origin` отвязан: форк никуда не пушит, пока не заведён свой репозиторий.

**2026-08-15 · Пакеты переименованы в `@resistance/*`.**
`bun.lock` перегенерирован. Больше на исходное имя ничего не ссылается.

**2026-08-15 · Оценка 10–12 дней посчитана из допущения «общение во внешнем
Discord».** Если решим встраивать чат или голос — переоценить.

---

## Открытые вопросы

| Вопрос | Статус |
| --- | --- |
| Общение: Discord, встроенный чат или голос | Открыт, блокирует оценку |
| Сколько времени давать лидеру на набор команды (30 сек из «Нечто» мало) | Открыт, решать в фазе 2 |
| Ограничивать ли фазу обсуждения таймером | Открыт, решать в фазе 2 |
| Судьба аналитического центра: переделывать сразу или после релиза | Открыт, решать в фазе 4 |
| Визуальный стиль: свои фоны, аватары и звук вместо хоррора «Нечто» | Открыт, в оценку не заложен |
